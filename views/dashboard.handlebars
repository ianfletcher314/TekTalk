<!-- this is the template for Home Page for some reasong this works even when comted out -->
<div>
    <div class="center card-panel no-margin row">
        {{!-- <h3 id="flow-text" class="grey-text text-darken-1 center logofont">
            SOUNDS
        </h3> --}}
        <div class="loopsContainer col s5 red">
            <div class="card-panel white scrollContainer">
            {{!-- {{#each post as |post|}}
            {{>singlepost}}
            {{/each}} --}}
            </div>
           
        </div>
        <div class="noiseCointainer col s5 offset-s2 blue">
          <div class="card-panel white scrollContainer">
            {{!-- {{#each post as |post|}}
            {{>singlepost}}
            {{/each}} --}}
            </div>
           
        </div>
        <div class="drumsCointainer col s5 green">
            <div class="card-panel white scrollContainer">
            {{!-- {{#each post as |post|}}
            {{>singlepost}}
            {{/each}} --}}
            </div>
           
        </div>
        <div class="samplesCointainer col s5 offset-s2 yellow">
            <div class="card-panel white scrollContainer">
            {{#each post as |post|}}
            {{>singlepost}}
            {{/each}}
            </div>
        </div>



    </div>

    {{!-- once comment rout is set up in home-routs.js this will generate the section --}}


</div>

<style type="text/css">
    .loopsContainer{
       height: 40vh;
       transform: translateX(7%);
    }
    .noiseCointainer{
        height: 40vh;
        transform: translateX(-7%);
    }
    .drumsCointainer{
        height: 40vh;
        transform: translateX(7%);
    }
    .samplesCointainer{
        height: 40vh;
        transform: translateX(-7%);
    }
  
    .scrollContainer{
        height:90%;
        margin-top: 2%;
    }
</style>