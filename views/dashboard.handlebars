<!-- this is the template for Home Page for some reasong this works even when comted out -->
<div>
    <div class="center card-panel no-margin row">
        {{!-- <h3 id="flow-text" class="grey-text text-darken-1 center logofont">
            SOUNDS
        </h3> --}}
        <div class="loopsContainer col s6 red">
            {{!-- {{#each post as |post|}}
            {{>singlepost}}
            {{/each}} --}}
           
        </div>
        <div class="noiseCointainer col s6 blue">
            {{!-- {{#each post as |post|}}
            {{>singlepost}}
            {{/each}} --}}
           
        </div>
        <div class="drumsCointainer col s6 green">
            {{!-- {{#each post as |post|}}
            {{>singlepost}}
            {{/each}} --}}
           
        </div>
        <div class="samplesCointainer col s6 yellow">
            {{!-- {{#each post as |post|}}
            {{>singlepost}}
            {{/each}} --}}
           
        </div>



    </div>

    {{!-- once comment rout is set up in home-routs.js this will generate the section --}}


</div>

<style type="text/css">
    .loopsContainer{
       height: 40vh;
    }
    .noiseCointainer{
        height: 40vh;
    }
    .samplesCointainer{
        height: 40vh;
    }
    .drumsCointainer{
        height: 40vh;
    }
</style>